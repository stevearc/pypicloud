language: python
python: "3.6"
services:
  - redis-server
  - mysql
  - postgresql
env:
  - TOXENV=py27
  - TOXENV=py34
  - TOXENV=py35
  - TOXENV=py36
install:
  - pip install tox coveralls
before_script:
  - mysql -e 'create database test;'
script: tox
after_success:
  if [ "$TOXENV" == "py27" ]; then coveralls; fi
